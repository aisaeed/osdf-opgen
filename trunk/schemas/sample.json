{
    "type": "object",
    "description": "Sample nodes describe physical samples collected from subjects or an environment. They are typically decorated with MIMS or MIGS.",
    "properties": {
        "migs_opgen": {
            "$ref": "migs_opgen"
        },
        "visit_number": {
            "type": "integer",
            "minimum": 1,
            "required": true
        },
        "fma_body_site": {
            "title": "Typically a term from the FMA ontology.",
            "type": "string",
            "required": false
        }
    },
    "additionalProperties": false
}
